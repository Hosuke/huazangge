# 华藏阁 — Stable deployment (no auto-learn on startup)

branding:
  name: "華藏閣"
  name_short: "華"
  tagline: "您的經典知識庫"
  powered_by_label: "Powered by LLMBase"
  powered_by_url: "https://github.com/Hosuke/llmbase"

llm:
  max_tokens: 8192

paths:
  raw: "./raw"
  wiki: "./wiki"
  outputs: "./wiki/outputs"
  meta: "./wiki/_meta"
  concepts: "./wiki/concepts"

compile:
  batch_size: 20
  backlinks: true

search:
  port: 5555

lint:
  web_search: false

worker:
  enabled: true
  learn_source: none
  learn_interval_hours: 999
  learn_batch_size: 0
  compile_interval_hours: 1
  taxonomy_interval_hours: 12
  health_check_interval_hours: 24

health:
  auto_fix_broken_links: true
  max_stubs_per_run: 10
